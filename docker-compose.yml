version: '3.3'

services:
  web:
    build:
      context: ./build
    image: nginx-php-dev:latest
    volumes:
      - ./www:/var/www/html
    ports:
      - mode: host
        target: 80
        published: 80
    links:
      - db
      - cache
  db:
    image: mysql:5.6
    environment:
      MYSQL_ROOT_PASSWORD: 123456
  cache:
    image: redis:latest